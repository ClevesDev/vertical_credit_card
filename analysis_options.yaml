# Configures the analyzer to check for errors, warnings, and lints.
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - doc/api/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
